import PropTypes from 'prop-types';

const propTypes = {
  
};

const defaultProps = {
   
};

const Checkox = (props: any) => {
    const { } = props;
    return (
        <div>

        </div>
    )
}

Checkox.propTypes = propTypes;
Checkox.defaultProps = defaultProps;

export default Checkox;