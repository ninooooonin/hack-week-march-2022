import PropTypes from 'prop-types';
import styled from 'styled-components';

import { AngleDownIcon, BorderRadiusIcon, MarginBottomIcon, MarginLeftIcon, MarginRightIcon, MarginTopIcon, UploadIcon } from '../general/icons';


const Container = styled.div`
    position: fixed;
    right: 0px;
    /* width: 100%; */
`

const propTypes = {
  
};

const defaultProps = {
   
};

const Select = (props: any) => {
    const { } = props;
    return (
        <div>

        </div>
    )
}

Select.propTypes = propTypes;
Select.defaultProps = defaultProps;

export default Select;